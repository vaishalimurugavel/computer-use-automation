# computer-use-automation
An LLM agent records reusable, versioned "capability" artifacts from exploring a UI, then replays them deterministically without re-invoking the model with typed error handling, risk-gated human escalation, and a feedback loop where human-resolved edge cases update the artifact itself.
